meta {
  name: healthCheck
  type: http
  seq: 3
}

get {
  url: {{payme-url}}/health
  body: none
  auth: inherit
}

vars:pre-request {
  localhost: localhost
  payme-url: http://localhost:8080
}

settings {
  encodeUrl: true
  timeout: 0
}
